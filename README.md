# Decode-lab-intership project 1-
# DecodeLabs Frontend Engineering Portfolio

Welcome to my DecodeLabs Frontend Development portfolio. This repository tracks my progression from a fundamental static web layout to an advanced, mobile-first, fluid responsive interface.

---

## 📂 Repository Structure

The portfolio is divided into two progressive milestone projects:

```text
├── Project-1_Static-Webpage/       # Foundation HTML & Basic CSS Desktop-First Grid
│   ├── index.html                  # Basic semantic structure
│   └── style.css                   # Inline-block styling & fallback media queries
│
└── Project-2_TechPulse/            # Advanced Mobile-First Responsive Interface
    ├── index (3).html              # HTML5 with native Popover API & semantic landmarks
    ├── style (4).css               # Fluid system with CSS Grid, Flexbox, & clamp() type
    └── script (1).js               # Intersection Observers & scroll count animations
